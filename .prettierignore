dist
dev-dist
coverage
node_modules
package-lock.json
*.png
*.ico
supabase/.temp/

# Markdown-dokumentation formateras inte av Prettier: docs och HANDOFF ägs av
# inception/dirigenten och ska inte få formaterings-churn från kod-grinden.
*.md

# FIFA Annexe C-källutdraget är en OFÖRÄNDRAD textextraktion (gold source som
# tabellen regenereras och diffas mot). Prettier har ingen parser för .txt och
# får aldrig röra den: minsta omformatering skulle bryta troheten mot PDF:en.
src/domain/bracket/annexe-c-source.txt

# Global-topplistans scoring-mirror är GENERERAD (esbuild-bundle av src-grafen,
# scripts/generate-global-leaderboard-core.ts, T90). Prettier får inte röra den:
# den regenereras ur src och vaktas av global-leaderboard-mirror-parity.test.ts;
# en formaterings-churn vore brus på ett artefakt ingen redigerar för hand.
supabase/functions/_shared/global-leaderboard-core.ts

# Mål-push-mirror:n är GENERERAD (esbuild-bundle av src-grafen,
# scripts/generate-goal-push-core.ts, T89). Prettier får inte röra den: den
# regenereras ur src och vaktas av goal-push-core-mirror-parity.test.ts; en
# formaterings-churn vore brus på ett artefakt ingen redigerar för hand.
supabase/functions/_shared/goal-push-core.ts

# Livescore-feature: de fångade API-Football-svaren är test-guld, committade
# OFÖRÄNDRADE I FORM (exakt som API:t levererade dem, en rad per svar). Prettier
# får inte röra dem: parsern (parse-live.ts) bevisas mot den FAKTISKA käll-formen,
# och en omformatering skulle dölja just det vi vill låsa (samma princip som
# gold-source-utdraget ovan).
src/data/livescore/__fixtures__/*.json

